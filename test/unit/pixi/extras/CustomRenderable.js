describe('CustomRenderable', function () {
    'use strict';

    var expect = chai.expect;

    it('Class exists', function () {
        expect(typeof PIXI.CustomRenderable).to.equal('function');
    });
});
